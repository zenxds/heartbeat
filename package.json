{
  "name": "heartbeat",
  "version": "0.1.0",
  "description": "heartbeat check",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=dev node index.js",
    "deploy": "NODE_ENV=prod pm2 start index.js --name heartbeat",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zenxds",
  "license": "ISC",
  "dependencies": {
    "config": "^1.26.1",
    "nodemailer": "^4.0.1",
    "pm2": "^2.5.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.4"
  }
}
